# Skill Forge Prompts

这里整理 Skill Forge 当前用于驱动较弱模型或轻量 Agent 的 prompt pack。

这些 prompt 的作用不是代替母本，而是让较弱模型在已有规则下完成：

- skill 压缩
- 路线同步
- 训练器框架生成
- 轻量更新与运维

使用原则：

- 先看对应 skill card
- 再用 prompt
- 不要跳过 source-of-truth
- 不要让 prompt 独立替代方法论母本
